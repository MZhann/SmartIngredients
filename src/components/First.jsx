import React from "react";

const First = ({ name, surname, age }) => {
    return (
        <div>
            <div>name: {name}</div>
            <div>surname: {surname}</div>
        </div>
    );
};

export default First;
